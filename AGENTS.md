# Agent Instructions

See **[CLAUDE.md](CLAUDE.md)** — it is the single source of truth for project
overview, repo layout, coding conventions, and common commands.

This file used to be a full copy, and it drifted: it still claimed the D1 schema
was generated from `spotify_core/db/schema.py` after the source of truth had
moved to `spotify_core/db/sql/schema.sql`, and it still listed a `scripts/`
directory that had been retired. One file, one truth.
