{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "setup",
  "description": "Capability for the Preferences (setup) window",
  "windows": [
    "setup"
  ],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
