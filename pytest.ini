[pytest]
minversion = 7.0
testpaths = tests
addopts = -ra
markers =
    unit: fast, isolated tests (no external services)
    integration: integration-level tests (multi-node / graph)
    llm: tests that invoke real LLMs or external AI services
